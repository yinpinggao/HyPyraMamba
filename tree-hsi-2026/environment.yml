name: tree-hsi-2026
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip=24.3
  - numpy=1.26.4
  - scipy=1.12.0
  - scikit-learn=1.4.2
  - h5py=3.10.0
  - pandas=2.2.2
  - pyyaml=6.0.1
  - tensorboard=2.16.2
  - matplotlib=3.8.4
  - pip:
      - einops==0.8.0
      - timm==1.0.7
      - kaggle==1.6.17
      # Install the validated torch 2.2 cxx11abiTRUE build, causal-conv1d, and
      # mamba-ssm only from precompiled wheels after environment creation. All
      # three wheels must agree on cu12/torch2.2/cxx11abiTRUE and CPython 3.10.
